Time

Every node is visited once-o(n)
	​

Space-o(n)

Stack can store up to height H

Worst case (skewed tree): H = N
import java.util.*;
class TreeNode{
    int val;
    TreeNode left;
    TreeNode right;
    public TreeNode(int val){
        this.left=null;
        this.right=null;
        this.val=val;
        
    }
    
}
public class Main
{
    private static List<Integer> iterative_preorder(TreeNode root){
        Stack<TreeNode>st=new Stack<>();
        ArrayList<Integer> li=new ArrayList<>();
        if(root==null) return li;
        st.push(root);
        while(!st.isEmpty()){
            TreeNode node=st.pop();
            li.add(node.val);
            if(node.right!=null){
                st.push(node.right);
            }
            if(node.left!=null){
              st.push(node.left);  
            }

        }
        return li;
        
    }
	public static void main(String[] args) {
		 TreeNode root=new TreeNode(1);
		 root.left=new TreeNode(2);
		 root.right=new TreeNode(3);
		 root.left.left=new TreeNode(4);
		 root.left.right=new TreeNode(5);
		 System.out.println(iterative_preorder(root));
		 
	}
}
